# Copy to apps/api/.env. Defaults are enough for local development.
NODE_ENV=development
HOST=127.0.0.1
PORT=3001
LOG_LEVEL=info
# Future database/auth/storage secrets belong only in this server workspace.
# No provider credentials or accounts are required for Phase 02.
