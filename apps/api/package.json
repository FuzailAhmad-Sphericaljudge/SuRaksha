{
  "name": "@suraksha/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.build.json && concurrently --kill-others -n api-build,api-server \"tsc -p tsconfig.build.json --watch --preserveWatchOutput\" \"node --watch --env-file-if-exists=.env dist/server.js\"",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "start": "node --env-file-if-exists=.env dist/server.js"
  },
  "dependencies": {
    "@fastify/multipart": "10.1.1",
    "@fastify/static": "10.1.3",
    "@suraksha/contracts": "0.1.0",
    "fastify": "5.12.4",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
